# Homebrew Tap

Add this tap with:
```console
$ brew tap jacquerie/tap
```

Then trust it with:
```console
$ brew trust jacquerie/tap
```

## Casks

### Nativ

Install with:
```console
$ brew install jacquerie/tap/nativ
```
